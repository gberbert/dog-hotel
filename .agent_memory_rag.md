# 🧠 Memória RAG do Agente

## Logs de Sessão
- **Data/Hora:** 2026-01-22T17:52:00-03:00
- **O que foi feito:** Inicialização da governança do sistema. Criação dos arquivos `.agent_memory_rag.md` e `.deploy_history.md`.
- **Motivo (O Porquê):** Adoção do novo `PROMPT DE INICIALIZAÇÃO` definido pelo usuário. Necessidade de rastreio de decisões e logs. Análise inicial detectou ausência de estrutura `/staging` e `/prd` física; operando na raiz como ambiente de desenvolvimento (STAGING LÓGICO) até decisão de restruturação.
- **Snapshot de Segurança:** N/A (Inicialização).

- **Data/Hora:** 2026-01-22T17:58:00-03:00
- **O que foi feito:** 
  1. Implementação de Alerta de Vacina Vencida (> 1 ano) com visualização em vermelho no Modal e Confirmação de Segurança ao Salvar.
  2. Correção Crítica no Cadastro de Clientes: Alterada lógica de detecção de duplicidade. Agora utiliza 'Chave Composta Lógica' (Nome do Pet + Nome do Tutor/WhatsApp). Permite cadastrar pets com mesmo nome se forem de tutores diferentes.
- **Motivo (O Porquê):** Pedido do usuário. O sistema anterior sobrescrevia dados de clientes diferentes se os pets tivessem o mesmo nome.
- **Snapshot de Segurança (Código Substituído em App.jsx):**
  ```javascript
  // Lógica Antiga (Bugada)
  const isDuplicate = clients.some(c => {
    const cName = (c.dogName || '').trim().toLowerCase();
    if (cName !== dogName) return false;
    // ... validação apenas de whatsapp
  });
  if (isDuplicate) return alert(...);
  // ...
  const existingClient = !clientId ?
    clients.find(c => c.dogName.toLowerCase() === formData.dogName.toLowerCase()) : ...
  ```
  ```

- **Data/Hora:** 2026-01-23T07:49:00-03:00
- **O que foi feito:** Adicionado alerta proativo (`alert()`) ao selecionar um cliente na busca rápida do modal de hospedagem, caso ele possua vacinas vencidas.
- **Motivo (O Porquê):** Solicitação do usuário para avisar imediatamente sobre vacinas vencidas ao carregar um cliente para nova hospedagem, reforçando o aviso que já existia no "Salvar".
- **Data/Hora:** 2026-01-23T07:52:00-03:00
- **O que foi feito:** Reposicionamento do bloco "Fotos do Pet" e "Carteira de Vacinação" no `BookingModal.jsx`. Movidos da coluna da direita (abaixo do Tutor) para a coluna da esquerda (logo abaixo dos Dados Básicos do Pet).
- **Motivo (O Porquê):** Melhoria da UX em Mobile. O usuário deseja ver as vacinas logo após os dados do cão, sem precisar rolar até o final da página (que seria onde ficava anteriormente).
- **Data/Hora:** 2026-01-23T08:04:00-03:00
- **O que foi feito:**
  1. Criação do componente `CustomAlert.jsx` (alerta modal estilizado).
  2. Substituição do `window.alert` nativo no `BookingModal` pelo novo `CustomAlert` ao detectar vacinas vencidas.
- **Motivo (O Porquê):** Pedido de UI específico: O alerta de vacinas deve ser visualmente impactante ("vermelho com ícone de exclamação amarelo"), o que não é possível com alertas nativos do navegador.

- **Data/Hora:** 2026-01-24T13:24:00-03:00
- **O que foi feito:** Criação do arquivo .stack_tech.md documentando a stack tecnológica (React 19, Vite, Tailwind, Firebase) e arquitetura do projeto.
- **Motivo (O Porquê):** Execução do fluxo de trabalho (.workflow.md) solicitado pelo usuário para regularizar a documentação do projeto.

- **Data/Hora:** 2026-01-24T13:30:00-03:00
- **O que foi feito:** Correção na lógica de notificação de medicação (`functions/index.js`). Alterada a consulta de `bookings` para filtrar `where('checkOut', '>=', todayStr)`.
- **Motivo (O Porquê):** Usuário relatou notificações para pets não hospedados. A busca anterior trazia todas as reservas da história. O filtro garante que apenas reservas ativas (ou futuras) sejam processadas, e a validação subsequente (`todayStr >= checkIn`) confirma a hospedagem atual.
- **Confirmação Lógica:** A verificação `isActive = (todayStr >= booking.checkIn && todayStr <= booking.checkOut)` já estava presente e correta, garantindo que o `checkOut` também seja respeitado.

- **Data/Hora:** 2026-01-25T12:55:00-03:00
- **O que foi feito:** Deploy da Cloud Function `checkScheduledAlerts` (Versão Node 20 / Firebase v12).
- **Motivo (O Porquê):** O usuário relatou que o bug persistia. Identificou-se que a correção no código não havia sido deployada para o ambiente cloud. O deploy foi realizado para efetivar as mudanças de filtro de data.

- **Data/Hora:** 2026-01-25T13:00:00-03:00
- **O que foi feito:** Adicionado botão "Limpar Tudo" na central de notificações (`NotificationBell.jsx`).
- **Motivo (O Porquê):** Solicitação do usuário para facilitar a remoção em massa de notificações acumuladas. Implementado via `writeBatch` do Firestore.

- **Data/Hora:** 2026-01-25T13:05:00-03:00
- **O que foi feito:** Refatoração de `NotificationBell.jsx` para incluir gestos de slide (Swipe Left) em dispositivos móveis.
- **Motivo (O Porquê):** Melhoria de UX/Mobile. Ao deslizar para a esquerda, opções de "Marcar como Lido" e "Excluir" são reveladas, imitando comportamento nativo de apps como WhatsApp/Email. Mantido botões de clique para Desktop.

- **Data/Hora:** 2026-01-25T13:10:00-03:00
- **O que foi feito:** Ajuste na lógica de repetição de alertas de remédios (`functions/index.js`).
- **Motivo (O Porquê):** O sistema notificava repetidamente durante toda a hora (`medHour === currentHour`). A lógica foi alterada para cálculo de minutos (`0 < diff <= 15`). Agora o alerta dispara apenas uma vez por ciclo de medicação, aproximadamente 10 minutos antes (considerando o jitter do cron). Deploy realizado.

- **Data/Hora:** 2026-01-25T13:25:00-03:00
- **O que foi feito:** Substituição do campo de seleção de horário de medicação: De Select (intervalos de 30min) para Input Time (HTML5).
- **Motivo (O Porquê):** Usuário solicitou liberdade para definir horários "quebrados" (ex: 13:27), o que não era possível com o dropdown fixo `00/30`. O `input type="time"` resolve isso nativamente com suporte a horas e minutos separados visualmente.

- **Data/Hora:** 2026-07-04T17:37:00-03:00
- **O que foi feito:** Correção do offset de timezone na função `getCapacityInfoForDate` em `src/utils/calculations.js`. O parsing de datas de bloqueio ('YYYY-MM-DD') passou a utilizar `split` para forçar a criação da data no timezone local ao invés de usar `new Date(string)` diretamente (que assumia UTC e causava recuo de 1 dia em horários locais como o do Brasil).
- **Motivo (O Porquê):** Usuário relatou que o "cadastro de bloqueio esta pegando 1 dia antes da data configurada". Devido ao fuso horário, o bloqueio do dia 12 estava caindo no dia 11 na visualização.
- **Snapshot de Segurança (Código Substituído em calculations.js):**
  ```javascript
  // Lógica Antiga (Bugada)
  const start = new Date(block.startDate);
  const end = new Date(block.endDate);
  ```
